endpoints and request params: 

resource: GET search-numbers
paramenters: vendor string, country_code int, area_code int

resource: GET buy_numbers
parameters: count int default 5 optional, vendor string default twilio optional

resource: DELETE delete_number
parameters: number string required

resourse: PUT update-number
body: number string, instance_id string, customer_id string, tenant_id string, user_id strgin

validation
numbers must be in E.164 format
tenant_id, customer_id, instance_id must be valid UUIDs
user must be a valid email address

response format:
JSON

authorization:
tls
jwt 
